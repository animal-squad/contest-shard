sharding:
  clusterRole: shardsvr

replication:
  replSetName: shard0

net:
  bindIp: 0.0.0.0
  port: ${SHARD0_PORT}

storage:
  dbPath: /data/shard0

systemLog:
  destination: file
  path: /logs/shard0.log
  logAppend: true

processManagement:
  fork: true
